# 更新日志

## 0.1.3 — 2026-09-20（焦点修复版）

### 新增

- **增加打开外部终端功能**
- **切屏焦点跟随**：Alt+Tab、切虚拟桌面、最小化到托盘再回来，光标会**精确回到上次编辑的
  那个输入框**（主界面与内嵌页面都支持），不用再重新点一下；中途也不会出现「先跳到页面
  左上角、再被拉回来」的闪烁。
- **数据跟着程序走**：运行期数据统一落在**程序目录**下，不再往 C 盘用户目录写东西。
  - 配置：`<程序目录>\data\store.json`
  - WebView2 浏览器数据（缓存 / Cookie / 登录态）：`<程序目录>\data\webview2\`
  - 老版本写在 `%APPDATA%\com.weblaunch.app\store.json` 的配置，首次启动**自动搬家**
    到新位置（老文件保留不动）。
  - 若程序目录不可写（例如装在 `C:\Program Files` 且没有管理员权限），会自动退回
    `%APPDATA%`，保证配置仍然存得下来。


### 变更

- **移除诊断日志**：焦点问题排查期间往 `%APPDATA%\com.weblaunch.app\focus.log` 写过
  激活链、窗口状态、页面 `activeElement` 等诊断信息；问题修复后整套删除，正常运行时
  不再写这个文件（`winfocus.rs` 模块头保留了「怎么再把它建起来」的说明）。
- 启动时的版本标记行（`winfocus: v5.7 boot: ...`）一并移除。